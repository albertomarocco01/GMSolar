import { AbsoluteFill } from "remotion";

export const FPS = 30;
export const TOTAL_DURATION = 30 * FPS; // placeholder — sostituito dalle scene

export const Presentation = () => {
  return <AbsoluteFill style={{ backgroundColor: "#ffffff" }} />;
};
